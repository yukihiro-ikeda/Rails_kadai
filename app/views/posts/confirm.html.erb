<h3>以下の内容で、送信する。</h3>

<%= form_with(model: @post, local: true) do |form| %>
  <%= form.hidden_field :title %>
  <%= form.hidden_field :content %>

  <p>本文:<%= @post.content %></p>
  <%= form.submit  "登録する" %>
<% end %>